# Logging Library

Production-grade structured logging library for archive system scripts.

## logger.sh

USB storage compliant logging with:
- Structured JSON logging
- Daily log rotation
- Component-based logging
- Log levels: INFO, WARN, ERROR
- Automatic log directory creation

Used by all Raspberry Pi scripts for consistent, production-grade logging.
